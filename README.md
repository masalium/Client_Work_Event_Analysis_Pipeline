# Client Work: CSV Event Analysis Pipeline
Automated pipeline that takes CSV files with event data and turns them into clear, structured summary tables. The tool groups events by location, category, and type, adapts automatically when new event types appear, and is built to be reused on recurring datasets with minimal manual work.
